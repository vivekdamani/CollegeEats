<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.collegeeats">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeEats">
        <activity android:name=".EditFoodItemActivity"></activity>
        <activity android:name=".PendingOrdersDetailsActivity" />
        <activity android:name=".PendingOrderDetailsActivity2" />
        <activity android:name=".PastOrderActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ProductViewHolder2" />
        <activity android:name=".AddItemActivity" />
        <activity android:name=".SellerOrdersActivity" />
        <activity android:name=".SellerMenuActivity" />
        <activity android:name=".SellerActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>